import 'package:flutter/material.dart';

class TechCourses extends StatelessWidget {
  const TechCourses({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: const Color.fromRGBO(251, 238, 230, 1),
    );
  }
}
